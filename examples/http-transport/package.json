{
  "name": "HTTP Transport",
  "description": "Learn how to set up an xmcp server using HTTP transport",
  "keywords": [
    "http"
  ],
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js",
    "validate": "xmcp validate"
  },
  "dependencies": {
    "xmcp": "workspace:*",
    "zod": "^4.0.10"
  }
}
